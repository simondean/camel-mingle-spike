subprojects {
    apply plugin: 'java'

    repositories {
       mavenCentral()
    }

    dependencies {
    }

    sourceCompatibility = 1.8

    version = '0.0.1'

    jar {
        manifest.attributes provider: 'gradle'
    }
}
